<mvc:View
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:viz="sap.viz.ui5.controls"
    xmlns:viz.data="sap.viz.ui5.data"
    xmlns:viz.feeds="sap.viz.ui5.controls.common.feeds"
    controllerName="zms.App">
    <Button text="jjjj" press="onPress"/>
    <viz:VizFrame
        id="idVizFrame"
        uiConfig="{applicationSet:'fiori'}"
        height='300px'
        width="100%"
        vizType='timeseries_line'>
        <viz:dataset>
            <viz.data:FlattenedDataset data="{/milk}">
                <viz.data:dimensions>
                    <viz.data:DimensionDefinition
                        name="Name of the dimension as displayed in the chart"
                        value="{Date}"
                        dataType="date"/>
                </viz.data:dimensions>
                <viz.data:measures>
                    <viz.data:MeasureDefinition
                        name="Name of the measure as displayed in the chart"
                        value="{Revenue}"/>
                </viz.data:measures>
            </viz.data:FlattenedDataset>
        </viz:dataset>
        <viz:feeds>
            <viz.feeds:FeedItem
                uid="valueAxis"
                type="Measure"
                values="Name of the measure as displayed in the chart" />
            <viz.feeds:FeedItem
                uid="timeAxis"
                type="Dimension"
                values="Name of the dimension as displayed in the chart" />
        </viz:feeds>
    </viz:VizFrame>
</mvc:View>
